{
  "name": "endstreet-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Endstreet Eleven studio site + Voxpolia landing — voxel destruction from the game engine",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "bash scripts/deploy.sh"
  },
  "dependencies": {
    "three": "^0.165.0"
  },
  "devDependencies": {
    "@types/three": "^0.165.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
